# deepseek-sparse-attention

Minimal implementation of "DeepSeek Sparse Attention" in PyTorch with the core lightning indexer, token selection and gathering mechanism primarily focused on the continued pre-training phase upon dummy sequences.
